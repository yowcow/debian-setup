- name: set time zone
  file:
    src: /usr/share/zoneinfo/Asia/Tokyo
    dest: /etc/localtime
    state: link
    force: yes

- name: install packages
  apt:
    name:
      - autoconf
      - automake
      - bind9-dnsutils
      - bison
      - build-essential
      - ca-certificates
      - checkinstall
      - clang
      - cmake
      - cronolog
      - curl
      - finger
      - flex
      - g++
      - gawk
      - git
      - golang
      - gpg
      - htop
      - jq
      - links
      - lynx
      - man
      - netbase
      - pkg-config
      - ripgrep
      - rsync
      - ruby
      - ruby-dev
      - speedtest-cli
      - sqlite3
      - sqlite3-doc
      - sqlite3-pcre
      - tmux
      - traceroute
      - tree
      - tzdata
      - uuid-runtime
      - valgrind
      - whois
      - weechat
      - zip
      - zsh

- name: install apt lib packages
  apt:
    name:
      - libdb-dev
      - libxml2-dev
      - libxml2-utils

- include: awscli.yml
- include: erlang.yml
- include: google-cloud.yml
- include: mysql.yml
- include: neovim.yml
- include: nodejs.yml
- include: perl.yml
- include: php.yml
- include: protobuf.yml
- include: psql.yml
- include: python.yml
- include: rust.yml
- include: travis.yml
- include: sysstat.yml
- include: universal-ctags.yml
- include: zig.yml
